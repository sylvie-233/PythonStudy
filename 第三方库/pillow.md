# pillow


图像处理库



```yaml
PIL:
    ImageEnhance: # 图片增强器
        Brightness:
        Color:
        Contrast:
        Sharpness:
    Image:
        filename:
        format:
        info:
        mode:
        size:
        convert(): # 模式转换
        crop(): # 剪切
        getpixel(): # 获取像素点
        open():
        resize(): # 调整大小
        rotate(): # 旋转
        save(): # 文件保存
    ImageDraw: # 图片绘制
        Draw:
        ellipse():
        line():
        multiline_text():
        rectangle():
    ImageFilter: # 图片滤镜
    ImageFont:
    ImageQt:
```