# loguru


## 核心内容
```yaml
loguru:
    logger:
        add(): # 添加处理器
            format:
            level:
        bind():
        catch():
        contextualize(): # 上下文管理
        exception():
        error():
        info():
        remove(): # 移除默认的处理器
        warning():
```