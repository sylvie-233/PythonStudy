# redis


## 核心内容
```yaml
redis:
    asyncio: # 异步io操作
        ConnectionPool: # 连接池
            from_url():
        Redis:
            connection_pool:
            db:
            host:
            port:
            close():
            get():
            set():
    Redis:
        
```