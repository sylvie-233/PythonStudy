# git

## 基础介绍

### git
```yaml
git:
    add: # 缓存stash
    branch: # 分支管理
        -d: # 删除分支
    check: # 切换分支
        -b: # 指定分支
    commit: # 提交
        -a: # 所有文件
        -m: # 备注信息
    config:
        --global: 
    diff:
    init: # 仓库初始化
    log:
    merge: # 分支合并
    push: # 推送
        -u: # 远程推送
    reflog:
    remote: # 远程仓库管理
        add: # 添加远程仓库
        show:
    reset:
    revert:
    stash:  
    status: # 仓库状态
```

## 核心内容